%h1 Photogram

-@posts.each do |post|
  .post
    .post-head
      .name
        Country AF
    .image.center-block
      =link_to (image_tag post.image.url(:medium), class: "img-responsive"), post_path(post) 
    %p.caption
      = sanitize post.caption